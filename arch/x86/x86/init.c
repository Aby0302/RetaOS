#include <stdint.h>

// Simple initialization function that gets called from assembly
void _init(void) {
    // This function is intentionally left empty as it's just a stub
    // for the linker. Actual initialization happens in kernel_init()
}
